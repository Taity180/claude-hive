{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Claude Hive window. Grants core plugin defaults (path, event, window, webview, app, image, resources, menu, tray) plus notification for attention alerts.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
